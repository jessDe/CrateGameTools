<Window x:Class="CrateGameTools.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CrateGameTools"
        mc:Ignorable="d"
        Title="CrateGameTools Overlay" Height="100" Width="Auto"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="False" ShowInTaskbar="False" ResizeMode="NoResize">
    <Border Background="#AA000000" CornerRadius="0,0,10,10" Padding="15,5" BorderBrush="#33FFFFFF" BorderThickness="0,0,0,1" HorizontalAlignment="Stretch">
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
            <!-- Main Timer -->
            <StackPanel Margin="10,0" VerticalAlignment="Center">
                <TextBlock Text="Timer" FontSize="10" Foreground="#AAAAAA" HorizontalAlignment="Center"/>
                <TextBlock x:Name="OverlayMainTimer" Text="00:00:00" FontSize="18" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
            </StackPanel>

            <!-- Vending Timer -->
            <StackPanel Margin="10,0" VerticalAlignment="Center">
                <TextBlock Text="Vending" FontSize="10" Foreground="#AAAAAA" HorizontalAlignment="Center"/>
                <TextBlock x:Name="OverlayVendingTimer" Text="900s" FontSize="18" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
            </StackPanel>

            <!-- Booster Timers (Dynamic) -->
            <ItemsControl x:Name="OverlayBoostersList" VerticalAlignment="Center">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Margin="10,0">
                            <TextBlock Text="{Binding Name}" FontSize="10" Foreground="#AAAAAA" HorizontalAlignment="Center"/>
                            <TextBlock Text="{Binding RemainingSeconds, StringFormat={}{0}s}" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="White"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsCooldown}" Value="True">
                                                <Setter Property="Foreground" Value="#FF5555"/>
                                            </DataTrigger>
                                            <MultiDataTrigger>
                                                <MultiDataTrigger.Conditions>
                                                    <Condition Binding="{Binding IsCooldown}" Value="False"/>
                                                    <Condition Binding="{Binding IsRunning}" Value="False"/>
                                                    <Condition Binding="{Binding RemainingSeconds}" Value="0"/>
                                                </MultiDataTrigger.Conditions>
                                                <Setter Property="Foreground" Value="#FF5555"/>
                                            </MultiDataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <!-- Last Save -->
            <StackPanel Margin="10,0" VerticalAlignment="Center">
                <TextBlock Text="Last Save" FontSize="10" Foreground="#AAAAAA" HorizontalAlignment="Center"/>
                <TextBlock x:Name="OverlayLastSave" Text="No saves" FontSize="18" FontWeight="Bold" Foreground="#32CD32" HorizontalAlignment="Center"/>
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
